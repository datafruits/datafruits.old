<% show = next_scheduled_show %>
<% if show %>
  NEXT LIVE SHOW: <%= "#{show.title || show.user.username} - #{show.time.strftime("%A %H:%M %Z")}" %>
  <% if show.user.avatar %>
    <% image_tag show.user.avatar %>
  <% end %>
<% end %>
